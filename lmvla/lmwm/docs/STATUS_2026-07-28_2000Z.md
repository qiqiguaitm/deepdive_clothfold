# 2026-07-28 当前全景:在飞任务 / 判决 / 坑 / 下一步

> 更新至 20:30 UTC。所有判决级结果落盘于 `lmvla/lmwm/docs/RESULTS_*_2026-07-28.md`。

## 1. ⭐ 核心判决(LIBERO 4-suite)——三臂对照已闭合(除 abs 待评)

### 1.1 全表(同协议 osmesa)

| 套件 | nowm | dual2q绝对 | 残差 | **shapeB(梯度隔离)** | abs025 |
|---|---|---|---|---|---|
| libero_10 | **94.55±0.50** | 95.2† | 95.75† | 95.30±0.77 | **待评** |
| libero_goal | 98.3 | 98.3 | 97.80±0.28 | 98.00±0.85 | — |
| libero_object | 99.8 | 99.5 | 99.65±0.19 | 99.80±0.23 | — |
| libero_spatial | 96.3 | 93.3 | 94.4 (n=8) | 95.85±1.18 | **待评** |
| **聚合** | **97.24** | 96.58 | 96.90 | **97.24** | — |
| Δ vs nowm | — | −0.62 | −0.30 | **0.00** | — |

† 绝对/残差的 libero_10 是 egl,补齐 osmesa 后可精修(暂影响 <+0.3)。

### 1.2 三个臂的结论(已锁)

| 臂 | 做什么 | 聚合 | Δ vs nowm | 论文定位 |
|---|---|---|---|---|
| dual2q绝对 | naive LMWM 全量注入 | 96.58 | −0.62 | baseline,有害 |
| 残差 | milestone−h_t 去冗余 | 96.90 | −0.30 | 半修复,精度旋钮 |
| **shapeB(梯度隔离)** | 两通道 target 全 detach | **97.24** | **0.00** | ⭐**主修复** |
| abs025 | GT×0.25 反衰减对照 | **待评** | — | P0-B 判决器 |

**spatial 赤字收敛链(机制最强证据)**:−3.0→−1.9→−0.45。梯度污染→残差半修→detach 根除。

### 1.3 关键 caveat(诚实补上)
- 聚合差 0.00 在 seed std 0.8~1.2 下不可声称显著。写"打平",不写"超越"。
- LIBERO = 饱和/零和诊断场。净增益正信号靠 RoboTwin。
- 绝对/残差的 libero_10 与 nowm/shapeB 非同协议(egl vs osmesa),±0.25 上未堵,后续可补但优先级低。

详:`RESULTS_shapeB_4suite_2026-07-28.md`、`RESULTS_P1F_resid_4suite_2026-07-28.md`

## 2. 在飞任务(按集群排序)

### 北京 North-H20

| job | 内容 | 状态 | 进度/ETA |
|---|---|---|---|
| vlv5h | #39 a0 pi05 RoboTwin eval(bridge+baseline) | Running | — |
| x4x6l | #39 a1 pi05 RoboTwin 训练(hint+30k) | **Queueing** | 等8卡 |
| ltlrl/5gzdk/8xvl8/dxtm4 | **P0-B abs025 eval**(l10+spatial×4seed) | Queueing | 单卡,ETA 2h |
| rlq6x | P0-B resid_noTsS4 训练(第二个残差臂) | Running | 9654/12500 **77%**,ETA 1.5h |
| zhf6h/jfgpm/jjhwj/kghcn | nowm l10 osmesa×4 | **全 Completed** | ✅ |

### 上海 East-H20

| job | 内容 | 状态 | 进度/ETA |
|---|---|---|---|
| tf4qx | **N2 残差 seed2 训练** | Running | 11182/12500 **89%**,ETA 40min |

### #39 pi05 RoboTwin 路径(阻塞级,依赖 a1 ckpt)

| 步骤 | 状态 | 备注 |
|---|---|---|
| a0 baseline eval | Running(vlv5h) | 4卡,openpi bridge,已验证归一化正确 |
| a1 hint 提取+训练 | Queueing(x4x6l) | 两修:glob→find + export_pi05_hint.py 同步 |
| a1 eval | 待 a1 ckpt | yaml 已备,auto-trigger watcher 已挂 |

## 3. 今天踩的坑(全部固化进模板+记忆)

| # | 坑 | 后果 | 修法 |
|---|---|---|---|
| 1 | eval bridge 文件只落 gf0,North-E 无 | a0 静默 fallback starvla→空跑 | 提交前两边 `grep -c 关键字` |
| 2 | shell `VAR=x` 不 export,heredoc `os.environ['VAR']` KeyError | a0 聚合空表 | entrypoint 一律 `export VAR=` |
| 3 | LaWAM ckpt run_dir 只有 .pt,缺伴生(config.yaml等) | shapeB eval AssertionError 40min假完成 | scp 前 `ls run_dir` 两边对比 |
| 4 | 27500文件 glob `ls ep*.npz` 超限→0→FATAL | a1 两崩(误判 grid 缺失) | `find $D -maxdepth 1 -name '*.npz' \| wc -l` |
| 5 | export_pi05_hint.py 没同步到 North-E | a1 三崩注 | **任何新脚本两边都要核对** |

## 4. P0-B 判决即将到来(最关键的对照)

**abs025 eval**(4×单卡,SUITES=libero_10+spatial,LMWM_MS_ABS_SCALE=0.25)约 2h 出。

| 场景 | abs025 l10/spatial 表现 | 含义 |
|---|---|---|
| abs < baseline | 直接 scale GT target 伤性能(有用信号被压) | **残差修复成立**(残差选对冗余方向,非全局压缩) |
| abs ≈ baseline | scale 不伤 | 残差=另一等价形式,弱化但可接受 |
| abs ≈ 残差 | 纯幅调参数 | **残差非独特**,降级为调参 |

结合 resid_noTsS4(1.5h后完成),出完整 P0-B 对照表。

## 5. N2 第二训练种子(40min 后完成)

目的:验证 residual +1.55 不是幸运种子。判据:seed2 residual 与首次 ≥95 同区间→稳健;崩到<94→降级。

## 6. 下一步(按依赖关系排序)

1. **现在**:等 abs eval(2h)+ N2(40min)+ P0-B resid(1.5h)三路判决齐→更新 1.2 表
2. **等 a1 ckpt**:pi05 RoboTwin a1 训练完(30k步≈40h)→提 a1 eval→**#39 判决**
3. **P0-C 门控训练**(plc6k 已完成,E-W 也可读 ckpt 评测,或等 #39 判决后决定方向)
4. **RoboDojo/LIBERO 协议补测**:绝对/残差 libero_10 osmesa 补测(低优先级,±0.25)
5. **论文写作**:PAPER_PLAN → paper-write,骨架已定("两根因+两修复+RoboTwin 正信号")
